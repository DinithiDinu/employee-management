# Day 1 – Employee Data Processing

## Run
python employee_data_processing.py

## Decisions
- Employees with salary `None` are skipped when calculating the average.
- If there are no valid salaries, the average function returns `None`.
